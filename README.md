# TinySQLDb
TinySQLDb is a simple relational database engine built with React and C++. It includes a web client, REST API, query processor, binary storage manager, system catalog, SQL command support, and in-memory BST/B-Tree indexes.
